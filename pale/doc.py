
def run_pale_doc():
  print "hello"


def generate_docs(module, output_format):
    """Returns the generated documentation, which you might like to print
    out directly, write to a file, or pipe to some other tool.
    """
    
